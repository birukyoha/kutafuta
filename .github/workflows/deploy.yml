name: Deploy to Production via FTP

on:
  push:
    branches: [ main ]

jobs:
  web-deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies and build
        run: |
          npm install
          npm run build

      - name: Copy PHP backend & config files into dist/
        run: |
          # Copy root-level PHP/config files into the dist folder
          cp api.php dist/api.php
          cp .htaccess dist/.htaccess
          # Copy the api/ PHP folder (config.php + index.php)
          cp -r api/ dist/api/
          # Copy db_config.php.example for reference (actual credentials stay in api/config.php)
          cp db_config.php.example dist/db_config.php.example

      - name: Deploy dist/ + PHP backend to Bluehost via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: dist/
          server-dir: /public_html/kutafutatalent.com/
          dangerous-clean-slate: false